# Selenium Assertion Review

Tests should verify expected outcomes after actions.

## Evidence

- No `expect(...)`, `assert(...)`, `should(...)`, or equivalent assertion.

## Recommendation Intent

Assert the expected UI state, URL, text, data, or persisted result.
